using System;
using System.Collections.Generic;
using System.Text;

namespace Omni
{
    public enum MessageDestinationType { User = 1, Group = 2 }
    public enum TranslationDestinationType { Public = 0, User = 1, Group = 2 }
    public enum TranslationDataType { Request = 0, Full = 1 }
}
