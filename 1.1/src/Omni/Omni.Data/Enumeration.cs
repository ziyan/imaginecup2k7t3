using System;
using System.Collections.Generic;
using System.Text;

namespace Omni.Data
{
    public enum MsgDstType { User = 1, Group = 2 }
    public enum TransDstType { Public = 0, User = 1, Group = 2 }
    public enum TransType { Request = 0, Full = 1 }
}
